# 项目进度

## 目标

实现 Chrome Extension (ChatFree) 通过本地 HTTP Server 调用 Windows SendInput API，完成 AI 平台的自动粘贴与发送。

```
Chrome Extension → HTTP POST JSON → input_server.exe → SendInput → 浏览器
```

## 当前阶段：Phase 1（最小可用版本 v0.1）

## 已完成

| # | 任务 | 状态 |
|---|---|---|
| 1 | C# 项目骨架（Kestrel + Native AOT） | ✅ |
| 2 | SendInput P/Invoke（key/type/click/wait） | ✅ |
| 3 | CommandModels + JSON 源生成器 | ✅ |
| 4 | server-client.js 通信模块 | ✅ |
| 5 | input-area.js 集成粘贴发送流程 | ✅ |
| 6 | content-ui.js focusPageInput 焦点回归 | ✅ |
| 7 | API 测试：健康检查 | ✅ |
| 8 | API 测试：JSON 大小写修复 | ✅ |
| 9 | API 测试：wait/空 actions/未知指令/缺字段 | ✅ |
| 10 | 面板高度：默认 15 行 + 底部 resize 拖拽 | ✅ |
| 11 | Debug 工具栏：7 个测试按钮（Health/Wait/Type/Paste/Full/Focus/Collapse） | ✅ |
| 12 | Debug 面板顶部 resize 拖拽 | ✅ |
| 13 | Server 日志升级（时间戳分隔线 + 逐 action 日志 + 耗时） | ✅ |
| 14 | MV3 fix：fetch 改为 background service worker relay | ✅ |
| 15 | CORS 中间件（Access-Control-Allow-Origin） | ✅ |
| 16 | Extension Debug 测试：Health / Wait / Type / Paste / Full 全部通过 | ✅ |

## 已知问题

| # | 问题 | 说明 |
|---|---|---|
| B1 | SendInput 导致卡死 | Type/Paste/Full 测试时，按键注入可能注入到自身或不确定窗口，导致浏览器/server 卡死，需强制退出。需要确保焦点在目标窗口后再注入。 |

## 待完成

| # | 任务 | 说明 |
|---|---|---|
| T1 | SendInput 卡死修复 | 注入前确保焦点在正确目标；或增加注入前确认机制 |
| T2 | 端到端测试：输入文字 → Enter → 粘贴+发送 | 需要解决 B1 后方可进行 |
| T3 | type 文本输入端到端验证 | |
| T4 | click 鼠标点击端到端验证 | |
| T5 | 降级测试：server 未启动时静默回退到仅复制 | |

## 关键文件

```
server/
├── Program.cs              # HTTP 入口，路由分发
├── InputExecutor.cs        # 指令执行引擎
├── NativeMethods.cs        # SendInput P/Invoke
├── CommandModels.cs        # JSON 数据模型 + 源生成器
├── InputServer.csproj      # 项目文件（PublishAot）

Extension:
├── modules/server-client.js # Server 通信客户端（relay through bg）
├── modules/input-area.js    # 集成粘贴发送 + serverClient 暴露
├── content-ui.js            # 面板注入 + 拖拽移动 + resize
├── background.js            # 图标点击 + server-relay 代理
├── app.js                   # Debug 测试按钮 + 面板 resize
├── index.html               # Debug 工具栏 HTML
```

## 下次继续

1. **修复 SendInput 卡死**：Type/Paste/Full 按钮触发的 SendInput 会卡死浏览器和 server。需要增加注入前焦点确认，确保按键发到目标窗口而非自身
2. 焦点确认完成后，重跑 Type → Paste → Full → 完整业务流程
